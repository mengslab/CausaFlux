# Intervention Generalization demo

Runs the v1.5 synthetic software benchmark for unseen perturbations, doses, combinations, and temporal sequences. CPA, GEARS, TxPert and scGPT are exposed through external prediction adapters; the bundled proxy baselines are interface regression models and must not be interpreted as executions of the published methods.
