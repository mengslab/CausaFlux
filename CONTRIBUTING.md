# Contributing

1. Create changes against a new branch.
2. Preserve donor-aware validation and synthetic-data disclosure.
3. Add tests for every new output contract.
4. Run `python -m pytest` and `python scripts/verify_release.py reference_demo`.
5. Update documentation, release notes, and the changelog.
6. Do not add patient-identifiable data, credentials, or licensed datasets to the repository.
