# Multimodal dynamic state quickstart

Runs the v1.4.0 multimodal benchmark with reduced settings. The bundled fixture is synthetic and exists only to verify modality-specific encoders, PoE/MoE fusion, modality dropout, cross-modal decoding, missingness sensitivity, and gate enforcement.
