# CausaFlux v1.9.0 — Virtual Cell Architecture

```text
Real-world / experimental inputs
            │
            ▼
   Data & provenance hub
            │
            ▼
┌──────────────────────────────────────────┐
│       Reliability-weighted AI router     │
│                                          │
│  Dynamic state       Multimodal fusion   │
│  Intervention        Tissue context      │
│  Foundation          Prospective UQ      │
└──────────────────────────────────────────┘
            │
            ▼
 Dynamic latent virtual-cell state
            │
            ├── proteostasis capacity
            ├── mitochondrial reserve
            ├── inflammatory dysfunction
            ├── commitment risk
            └── recovery potential
            │
            ▼
 Counterfactual intervention scenarios
            │
            ▼
 Cost + uncertainty-aware recommendation
            │
            ▼
 Preregistered / model-frozen experiment
            │
            ▼
 QC → locked outcome → posterior update
            │
            └─────────────── feedback ─────┘
```

The v1.9 router is deliberately modular. A dataset-specific future release can replace any reference component with a trained model as long as it satisfies the corresponding input/output and prospective-validation contracts.
