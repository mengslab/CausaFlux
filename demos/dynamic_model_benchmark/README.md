# Dynamic model benchmark quickstart

Runs the v1.4.0 held-out perturbation-history benchmark with reduced training settings:

```bash
sh demos/dynamic_model_benchmark/run.sh
```

The fixture is synthetic and tests model behavior, leakage controls, calibrated likelihood, fate prediction, and gate enforcement. Passing this software gate does not authorize foundation pretraining or establish biological performance.
